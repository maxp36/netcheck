{
    db: {
        host: host
        user: user
        pass: pass
        name: name
    }

    hosts: [
        host1
        host2
    ]

    aliases: {
        host_name: host
    }

    limitations: {
        host1: {
            check_ports: [port1, port2]
            exclude_ports: [port3]
        }
        host2: {
            exclude_ports: [port1]
        }
    }
}