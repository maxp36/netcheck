{
  db: {
    host: host
    user: user
    pass: pass
    name: name
  }

  hosts: [
    {
      host: host
      limitations: [
        {
          host: host
          aliases: [
            alias1
            alias2
          ]
          check_ports: [port1, port2]
          exclude_ports: [port3]
        }
      ]
    }
  ]
}